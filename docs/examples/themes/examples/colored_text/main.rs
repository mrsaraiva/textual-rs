use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'colored_text' from Python source 'themes/colored_text.py' (category 'themes')."
    );
    Ok(())
}
