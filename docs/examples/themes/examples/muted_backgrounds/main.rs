use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'muted_backgrounds' from Python source 'themes/muted_backgrounds.py' (category 'themes')."
    );
    Ok(())
}
