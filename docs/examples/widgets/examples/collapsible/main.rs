use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'collapsible' from Python source 'widgets/collapsible.py' (category 'widgets')."
    );
    Ok(())
}
