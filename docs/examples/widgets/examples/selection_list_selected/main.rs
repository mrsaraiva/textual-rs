use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'selection_list_selected' from Python source 'widgets/selection_list_selected.py' (category 'widgets')."
    );
    Ok(())
}
