use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'selection_list_tuples' from Python source 'widgets/selection_list_tuples.py' (category 'widgets')."
    );
    Ok(())
}
