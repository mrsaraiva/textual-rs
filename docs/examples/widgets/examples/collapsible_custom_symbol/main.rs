use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'collapsible_custom_symbol' from Python source 'widgets/collapsible_custom_symbol.py' (category 'widgets')."
    );
    Ok(())
}
