use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'content_switcher' from Python source 'widgets/content_switcher.py' (category 'widgets')."
    );
    Ok(())
}
