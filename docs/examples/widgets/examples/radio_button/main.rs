use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'radio_button' from Python source 'widgets/radio_button.py' (category 'widgets')."
    );
    Ok(())
}
