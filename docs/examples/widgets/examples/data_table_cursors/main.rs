use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'data_table_cursors' from Python source 'widgets/data_table_cursors.py' (category 'widgets')."
    );
    Ok(())
}
