use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'header_app_title' from Python source 'widgets/header_app_title.py' (category 'widgets')."
    );
    Ok(())
}
