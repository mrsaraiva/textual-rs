use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'directory_tree_filtered' from Python source 'widgets/directory_tree_filtered.py' (category 'widgets')."
    );
    Ok(())
}
