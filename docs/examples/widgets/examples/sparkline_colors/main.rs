use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'sparkline_colors' from Python source 'widgets/sparkline_colors.py' (category 'widgets')."
    );
    Ok(())
}
