use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'horizontal_rules' from Python source 'widgets/horizontal_rules.py' (category 'widgets')."
    );
    Ok(())
}
