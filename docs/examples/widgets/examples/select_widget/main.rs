use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'select_widget' from Python source 'widgets/select_widget.py' (category 'widgets')."
    );
    Ok(())
}
