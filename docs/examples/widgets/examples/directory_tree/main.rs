use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'directory_tree' from Python source 'widgets/directory_tree.py' (category 'widgets')."
    );
    Ok(())
}
