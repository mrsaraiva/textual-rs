use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'option_list_strings' from Python source 'widgets/option_list_strings.py' (category 'widgets')."
    );
    Ok(())
}
