use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'option_list_options' from Python source 'widgets/option_list_options.py' (category 'widgets')."
    );
    Ok(())
}
