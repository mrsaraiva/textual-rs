use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'select_from_values_widget' from Python source 'widgets/select_from_values_widget.py' (category 'widgets')."
    );
    Ok(())
}
