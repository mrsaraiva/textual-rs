use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'selection_list_selections' from Python source 'widgets/selection_list_selections.py' (category 'widgets')."
    );
    Ok(())
}
