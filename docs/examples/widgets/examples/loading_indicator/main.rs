use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'loading_indicator' from Python source 'widgets/loading_indicator.py' (category 'widgets')."
    );
    Ok(())
}
