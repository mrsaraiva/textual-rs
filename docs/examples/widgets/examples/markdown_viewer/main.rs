use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'markdown_viewer' from Python source 'widgets/markdown_viewer.py' (category 'widgets')."
    );
    Ok(())
}
