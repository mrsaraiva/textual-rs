use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'option_list_tables' from Python source 'widgets/option_list_tables.py' (category 'widgets')."
    );
    Ok(())
}
