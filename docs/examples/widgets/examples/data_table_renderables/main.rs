use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'data_table_renderables' from Python source 'widgets/data_table_renderables.py' (category 'widgets')."
    );
    Ok(())
}
