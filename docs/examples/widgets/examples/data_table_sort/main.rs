use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'data_table_sort' from Python source 'widgets/data_table_sort.py' (category 'widgets')."
    );
    Ok(())
}
