use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'vertical_rules' from Python source 'widgets/vertical_rules.py' (category 'widgets')."
    );
    Ok(())
}
