use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'collapsible_nested' from Python source 'widgets/collapsible_nested.py' (category 'widgets')."
    );
    Ok(())
}
