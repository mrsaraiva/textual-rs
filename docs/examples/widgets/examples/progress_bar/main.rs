use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'progress_bar' from Python source 'widgets/progress_bar.py' (category 'widgets')."
    );
    Ok(())
}
