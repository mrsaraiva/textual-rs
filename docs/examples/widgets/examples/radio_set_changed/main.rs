use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'radio_set_changed' from Python source 'widgets/radio_set_changed.py' (category 'widgets')."
    );
    Ok(())
}
