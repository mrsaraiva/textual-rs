use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'progress_bar_gradient' from Python source 'widgets/progress_bar_gradient.py' (category 'widgets')."
    );
    Ok(())
}
