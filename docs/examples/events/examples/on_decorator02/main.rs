use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'on_decorator02' from Python source 'events/on_decorator02.py' (category 'events')."
    );
    Ok(())
}
