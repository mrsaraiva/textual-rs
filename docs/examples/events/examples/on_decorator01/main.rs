use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'on_decorator01' from Python source 'events/on_decorator01.py' (category 'events')."
    );
    Ok(())
}
