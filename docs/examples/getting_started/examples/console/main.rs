use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'console' from Python source 'getting_started/console.py' (category 'getting_started')."
    );
    Ok(())
}
