use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'animation01' from Python source 'guide/animator/animation01.py' (category 'guide/animator')."
    );
    Ok(())
}
