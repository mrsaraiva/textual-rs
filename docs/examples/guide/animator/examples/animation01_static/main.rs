use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'animation01_static' from Python source 'guide/animator/animation01_static.py' (category 'guide/animator')."
    );
    Ok(())
}
