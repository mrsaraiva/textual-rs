use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'command02' from Python source 'guide/command_palette/command02.py' (category 'guide/command_palette')."
    );
    Ok(())
}
