use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'command01' from Python source 'guide/command_palette/command01.py' (category 'guide/command_palette')."
    );
    Ok(())
}
