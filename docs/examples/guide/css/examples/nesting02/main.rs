use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'nesting02' from Python source 'guide/css/nesting02.py' (category 'guide/css')."
    );
    Ok(())
}
