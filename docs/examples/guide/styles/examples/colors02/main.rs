use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'colors02' from Python source 'guide/styles/colors02.py' (category 'guide/styles')."
    );
    Ok(())
}
