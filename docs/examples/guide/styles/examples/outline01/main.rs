use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'outline01' from Python source 'guide/styles/outline01.py' (category 'guide/styles')."
    );
    Ok(())
}
