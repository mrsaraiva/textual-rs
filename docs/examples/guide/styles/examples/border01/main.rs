use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'border01' from Python source 'guide/styles/border01.py' (category 'guide/styles')."
    );
    Ok(())
}
