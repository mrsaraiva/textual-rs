use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'widget' from Python source 'guide/styles/widget.py' (category 'guide/styles')."
    );
    Ok(())
}
