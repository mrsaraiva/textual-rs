use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'margin01' from Python source 'guide/styles/margin01.py' (category 'guide/styles')."
    );
    Ok(())
}
