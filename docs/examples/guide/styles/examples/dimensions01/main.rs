use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'dimensions01' from Python source 'guide/styles/dimensions01.py' (category 'guide/styles')."
    );
    Ok(())
}
