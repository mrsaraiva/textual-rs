use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'colors' from Python source 'guide/styles/colors.py' (category 'guide/styles')."
    );
    Ok(())
}
