use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'colors01' from Python source 'guide/styles/colors01.py' (category 'guide/styles')."
    );
    Ok(())
}
