use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'border_title' from Python source 'guide/styles/border_title.py' (category 'guide/styles')."
    );
    Ok(())
}
