use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'screen' from Python source 'guide/styles/screen.py' (category 'guide/styles')."
    );
    Ok(())
}
