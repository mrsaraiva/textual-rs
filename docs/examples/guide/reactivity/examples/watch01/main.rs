use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'watch01' from Python source 'guide/reactivity/watch01.py' (category 'guide/reactivity')."
    );
    Ok(())
}
