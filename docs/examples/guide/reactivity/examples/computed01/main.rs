use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'computed01' from Python source 'guide/reactivity/computed01.py' (category 'guide/reactivity')."
    );
    Ok(())
}
