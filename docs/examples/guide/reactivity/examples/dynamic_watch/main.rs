use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'dynamic_watch' from Python source 'guide/reactivity/dynamic_watch.py' (category 'guide/reactivity')."
    );
    Ok(())
}
