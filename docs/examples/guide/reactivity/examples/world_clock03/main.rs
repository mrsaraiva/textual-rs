use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'world_clock03' from Python source 'guide/reactivity/world_clock03.py' (category 'guide/reactivity')."
    );
    Ok(())
}
