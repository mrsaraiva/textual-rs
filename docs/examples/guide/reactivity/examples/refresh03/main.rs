use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'refresh03' from Python source 'guide/reactivity/refresh03.py' (category 'guide/reactivity')."
    );
    Ok(())
}
