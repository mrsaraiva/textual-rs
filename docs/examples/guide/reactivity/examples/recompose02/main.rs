use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'recompose02' from Python source 'guide/reactivity/recompose02.py' (category 'guide/reactivity')."
    );
    Ok(())
}
