use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'set_reactive03' from Python source 'guide/reactivity/set_reactive03.py' (category 'guide/reactivity')."
    );
    Ok(())
}
