use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'world_clock01' from Python source 'guide/reactivity/world_clock01.py' (category 'guide/reactivity')."
    );
    Ok(())
}
