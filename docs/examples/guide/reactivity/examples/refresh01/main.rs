use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'refresh01' from Python source 'guide/reactivity/refresh01.py' (category 'guide/reactivity')."
    );
    Ok(())
}
