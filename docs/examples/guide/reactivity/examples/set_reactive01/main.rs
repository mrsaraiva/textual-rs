use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'set_reactive01' from Python source 'guide/reactivity/set_reactive01.py' (category 'guide/reactivity')."
    );
    Ok(())
}
