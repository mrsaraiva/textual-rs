use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'refresh02' from Python source 'guide/reactivity/refresh02.py' (category 'guide/reactivity')."
    );
    Ok(())
}
