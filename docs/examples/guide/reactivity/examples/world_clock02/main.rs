use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'world_clock02' from Python source 'guide/reactivity/world_clock02.py' (category 'guide/reactivity')."
    );
    Ok(())
}
