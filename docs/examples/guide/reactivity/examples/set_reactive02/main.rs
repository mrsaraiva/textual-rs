use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'set_reactive02' from Python source 'guide/reactivity/set_reactive02.py' (category 'guide/reactivity')."
    );
    Ok(())
}
