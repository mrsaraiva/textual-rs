use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'screen01' from Python source 'guide/screens/screen01.py' (category 'guide/screens')."
    );
    Ok(())
}
