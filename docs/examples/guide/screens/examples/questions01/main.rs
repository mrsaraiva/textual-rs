use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'questions01' from Python source 'guide/screens/questions01.py' (category 'guide/screens')."
    );
    Ok(())
}
