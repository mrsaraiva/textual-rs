use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'modes01' from Python source 'guide/screens/modes01.py' (category 'guide/screens')."
    );
    Ok(())
}
