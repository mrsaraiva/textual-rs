use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'screen02' from Python source 'guide/screens/screen02.py' (category 'guide/screens')."
    );
    Ok(())
}
