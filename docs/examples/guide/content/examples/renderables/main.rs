use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'renderables' from Python source 'guide/content/renderables.py' (category 'guide/content')."
    );
    Ok(())
}
