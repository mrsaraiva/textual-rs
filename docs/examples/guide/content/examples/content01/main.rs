use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'content01' from Python source 'guide/content/content01.py' (category 'guide/content')."
    );
    Ok(())
}
