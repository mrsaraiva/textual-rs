use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'playground' from Python source 'guide/content/playground.py' (category 'guide/content')."
    );
    Ok(())
}
