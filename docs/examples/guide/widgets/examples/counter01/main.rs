use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'counter01' from Python source 'guide/widgets/counter01.py' (category 'guide/widgets')."
    );
    Ok(())
}
