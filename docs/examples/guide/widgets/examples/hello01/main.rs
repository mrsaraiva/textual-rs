use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'hello01' from Python source 'guide/widgets/hello01.py' (category 'guide/widgets')."
    );
    Ok(())
}
