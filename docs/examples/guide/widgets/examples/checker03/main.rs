use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'checker03' from Python source 'guide/widgets/checker03.py' (category 'guide/widgets')."
    );
    Ok(())
}
