use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'hello03' from Python source 'guide/widgets/hello03.py' (category 'guide/widgets')."
    );
    Ok(())
}
