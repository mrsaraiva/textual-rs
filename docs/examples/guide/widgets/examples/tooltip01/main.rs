use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'tooltip01' from Python source 'guide/widgets/tooltip01.py' (category 'guide/widgets')."
    );
    Ok(())
}
