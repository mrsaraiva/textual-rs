use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'hello05' from Python source 'guide/widgets/hello05.py' (category 'guide/widgets')."
    );
    Ok(())
}
