use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'tooltip02' from Python source 'guide/widgets/tooltip02.py' (category 'guide/widgets')."
    );
    Ok(())
}
