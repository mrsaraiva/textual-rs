use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'hello04' from Python source 'guide/widgets/hello04.py' (category 'guide/widgets')."
    );
    Ok(())
}
