use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'hello06' from Python source 'guide/widgets/hello06.py' (category 'guide/widgets')."
    );
    Ok(())
}
