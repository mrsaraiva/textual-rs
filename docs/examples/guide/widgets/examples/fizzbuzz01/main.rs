use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'fizzbuzz01' from Python source 'guide/widgets/fizzbuzz01.py' (category 'guide/widgets')."
    );
    Ok(())
}
