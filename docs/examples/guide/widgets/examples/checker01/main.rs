use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'checker01' from Python source 'guide/widgets/checker01.py' (category 'guide/widgets')."
    );
    Ok(())
}
