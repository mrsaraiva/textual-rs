use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'loading01' from Python source 'guide/widgets/loading01.py' (category 'guide/widgets')."
    );
    Ok(())
}
