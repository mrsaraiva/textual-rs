use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'hello02' from Python source 'guide/widgets/hello02.py' (category 'guide/widgets')."
    );
    Ok(())
}
