use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'checker02' from Python source 'guide/widgets/checker02.py' (category 'guide/widgets')."
    );
    Ok(())
}
