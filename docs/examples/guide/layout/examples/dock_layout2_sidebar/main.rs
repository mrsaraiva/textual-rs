use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'dock_layout2_sidebar' from Python source 'guide/layout/dock_layout2_sidebar.py' (category 'guide/layout')."
    );
    Ok(())
}
