use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'vertical_layout' from Python source 'guide/layout/vertical_layout.py' (category 'guide/layout')."
    );
    Ok(())
}
