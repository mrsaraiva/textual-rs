use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'horizontal_layout' from Python source 'guide/layout/horizontal_layout.py' (category 'guide/layout')."
    );
    Ok(())
}
