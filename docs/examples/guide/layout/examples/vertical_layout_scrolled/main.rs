use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'vertical_layout_scrolled' from Python source 'guide/layout/vertical_layout_scrolled.py' (category 'guide/layout')."
    );
    Ok(())
}
