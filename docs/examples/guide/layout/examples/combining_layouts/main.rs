use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'combining_layouts' from Python source 'guide/layout/combining_layouts.py' (category 'guide/layout')."
    );
    Ok(())
}
