use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'horizontal_layout_overflow' from Python source 'guide/layout/horizontal_layout_overflow.py' (category 'guide/layout')."
    );
    Ok(())
}
