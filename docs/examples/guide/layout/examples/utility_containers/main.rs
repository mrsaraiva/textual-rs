use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'utility_containers' from Python source 'guide/layout/utility_containers.py' (category 'guide/layout')."
    );
    Ok(())
}
