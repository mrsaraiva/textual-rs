use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'dock_layout1_sidebar' from Python source 'guide/layout/dock_layout1_sidebar.py' (category 'guide/layout')."
    );
    Ok(())
}
