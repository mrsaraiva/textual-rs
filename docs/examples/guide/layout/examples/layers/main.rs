use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'layers' from Python source 'guide/layout/layers.py' (category 'guide/layout')."
    );
    Ok(())
}
