use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'grid_layout2' from Python source 'guide/layout/grid_layout2.py' (category 'guide/layout')."
    );
    Ok(())
}
