use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'utility_containers_using_with' from Python source 'guide/layout/utility_containers_using_with.py' (category 'guide/layout')."
    );
    Ok(())
}
