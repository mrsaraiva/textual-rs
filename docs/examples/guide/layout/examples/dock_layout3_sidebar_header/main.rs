use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'dock_layout3_sidebar_header' from Python source 'guide/layout/dock_layout3_sidebar_header.py' (category 'guide/layout')."
    );
    Ok(())
}
