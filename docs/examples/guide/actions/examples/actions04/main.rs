use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'actions04' from Python source 'guide/actions/actions04.py' (category 'guide/actions')."
    );
    Ok(())
}
