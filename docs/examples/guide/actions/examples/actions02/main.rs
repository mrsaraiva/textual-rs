use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'actions02' from Python source 'guide/actions/actions02.py' (category 'guide/actions')."
    );
    Ok(())
}
