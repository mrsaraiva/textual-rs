use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'actions01' from Python source 'guide/actions/actions01.py' (category 'guide/actions')."
    );
    Ok(())
}
