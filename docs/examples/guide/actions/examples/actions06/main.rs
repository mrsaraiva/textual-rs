use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'actions06' from Python source 'guide/actions/actions06.py' (category 'guide/actions')."
    );
    Ok(())
}
