use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'actions05' from Python source 'guide/actions/actions05.py' (category 'guide/actions')."
    );
    Ok(())
}
