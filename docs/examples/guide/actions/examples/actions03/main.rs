use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'actions03' from Python source 'guide/actions/actions03.py' (category 'guide/actions')."
    );
    Ok(())
}
