use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'byte01' from Python source 'guide/compound/byte01.py' (category 'guide/compound')."
    );
    Ok(())
}
