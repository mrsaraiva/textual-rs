use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'byte02' from Python source 'guide/compound/byte02.py' (category 'guide/compound')."
    );
    Ok(())
}
