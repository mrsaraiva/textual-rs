use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'byte03' from Python source 'guide/compound/byte03.py' (category 'guide/compound')."
    );
    Ok(())
}
