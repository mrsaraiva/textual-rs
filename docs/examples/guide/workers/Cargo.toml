[package]
name = "textual-docs-guide-workers"
version = "0.1.0"
edition = "2024"
publish = false

[features]
http-examples = ["dep:ureq"]

[dependencies]
textual = { path = "../../../.." }
rich-rs = "1.1.0"
crossterm = "0.28"
tree-sitter-java = "0.23"
ureq = { version = "3", optional = true }

