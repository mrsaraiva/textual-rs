use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'weather02' from Python source 'guide/workers/weather02.py' (category 'guide/workers')."
    );
    Ok(())
}
