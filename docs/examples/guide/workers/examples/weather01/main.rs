use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'weather01' from Python source 'guide/workers/weather01.py' (category 'guide/workers')."
    );
    Ok(())
}
