use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'weather05' from Python source 'guide/workers/weather05.py' (category 'guide/workers')."
    );
    Ok(())
}
