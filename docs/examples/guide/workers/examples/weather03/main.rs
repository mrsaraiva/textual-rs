use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'weather03' from Python source 'guide/workers/weather03.py' (category 'guide/workers')."
    );
    Ok(())
}
