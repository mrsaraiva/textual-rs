use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'weather04' from Python source 'guide/workers/weather04.py' (category 'guide/workers')."
    );
    Ok(())
}
