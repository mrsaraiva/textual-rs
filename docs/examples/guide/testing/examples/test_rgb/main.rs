use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'test_rgb' from Python source 'guide/testing/test_rgb.py' (category 'guide/testing')."
    );
    Ok(())
}
