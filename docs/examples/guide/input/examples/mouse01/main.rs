use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'mouse01' from Python source 'guide/input/mouse01.py' (category 'guide/input')."
    );
    Ok(())
}
