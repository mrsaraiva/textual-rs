use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'binding01' from Python source 'guide/input/binding01.py' (category 'guide/input')."
    );
    Ok(())
}
