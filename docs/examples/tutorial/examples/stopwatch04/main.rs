use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'stopwatch04' from Python source 'tutorial/stopwatch04.py' (category 'tutorial')."
    );
    Ok(())
}
