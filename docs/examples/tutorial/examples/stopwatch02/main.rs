use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'stopwatch02' from Python source 'tutorial/stopwatch02.py' (category 'tutorial')."
    );
    Ok(())
}
