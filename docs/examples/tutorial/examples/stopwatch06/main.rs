use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'stopwatch06' from Python source 'tutorial/stopwatch06.py' (category 'tutorial')."
    );
    Ok(())
}
