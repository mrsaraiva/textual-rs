use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'stopwatch01' from Python source 'tutorial/stopwatch01.py' (category 'tutorial')."
    );
    Ok(())
}
