use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'stopwatch05' from Python source 'tutorial/stopwatch05.py' (category 'tutorial')."
    );
    Ok(())
}
