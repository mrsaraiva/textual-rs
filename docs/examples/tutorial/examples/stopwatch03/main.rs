use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'stopwatch03' from Python source 'tutorial/stopwatch03.py' (category 'tutorial')."
    );
    Ok(())
}
