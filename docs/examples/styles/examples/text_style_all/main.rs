use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'text_style_all' from Python source 'styles/text_style_all.py' (category 'styles')."
    );
    Ok(())
}
