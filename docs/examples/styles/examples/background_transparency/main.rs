use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'background_transparency' from Python source 'styles/background_transparency.py' (category 'styles')."
    );
    Ok(())
}
