use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'content_align' from Python source 'styles/content_align.py' (category 'styles')."
    );
    Ok(())
}
