use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'grid_size_both' from Python source 'styles/grid_size_both.py' (category 'styles')."
    );
    Ok(())
}
