use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'link_background' from Python source 'styles/link_background.py' (category 'styles')."
    );
    Ok(())
}
