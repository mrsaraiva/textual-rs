use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'scrollbar_visibility' from Python source 'styles/scrollbar_visibility.py' (category 'styles')."
    );
    Ok(())
}
