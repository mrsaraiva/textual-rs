use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'scrollbar_gutter' from Python source 'styles/scrollbar_gutter.py' (category 'styles')."
    );
    Ok(())
}
