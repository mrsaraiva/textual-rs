use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'link_background_hover' from Python source 'styles/link_background_hover.py' (category 'styles')."
    );
    Ok(())
}
