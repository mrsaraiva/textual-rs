use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'scrollbar_size' from Python source 'styles/scrollbar_size.py' (category 'styles')."
    );
    Ok(())
}
