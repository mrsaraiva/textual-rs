use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'text_opacity' from Python source 'styles/text_opacity.py' (category 'styles')."
    );
    Ok(())
}
