use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'link_style_hover' from Python source 'styles/link_style_hover.py' (category 'styles')."
    );
    Ok(())
}
