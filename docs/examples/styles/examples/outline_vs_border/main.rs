use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'outline_vs_border' from Python source 'styles/outline_vs_border.py' (category 'styles')."
    );
    Ok(())
}
