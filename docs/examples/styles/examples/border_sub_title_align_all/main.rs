use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'border_sub_title_align_all' from Python source 'styles/border_sub_title_align_all.py' (category 'styles')."
    );
    Ok(())
}
