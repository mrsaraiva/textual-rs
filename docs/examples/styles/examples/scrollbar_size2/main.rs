use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'scrollbar_size2' from Python source 'styles/scrollbar_size2.py' (category 'styles')."
    );
    Ok(())
}
