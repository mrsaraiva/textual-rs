use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'content_align_all' from Python source 'styles/content_align_all.py' (category 'styles')."
    );
    Ok(())
}
