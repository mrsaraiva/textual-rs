use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'height_comparison' from Python source 'styles/height_comparison.py' (category 'styles')."
    );
    Ok(())
}
