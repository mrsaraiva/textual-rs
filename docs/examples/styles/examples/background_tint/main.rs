use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'background_tint' from Python source 'styles/background_tint.py' (category 'styles')."
    );
    Ok(())
}
