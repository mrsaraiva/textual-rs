use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'link_color_hover' from Python source 'styles/link_color_hover.py' (category 'styles')."
    );
    Ok(())
}
