use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'suspend_process' from Python source 'app/suspend_process.py' (category 'app')."
    );
    Ok(())
}
