use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'question_title02' from Python source 'app/question_title02.py' (category 'app')."
    );
    Ok(())
}
