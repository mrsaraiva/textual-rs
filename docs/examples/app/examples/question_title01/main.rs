use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'question_title01' from Python source 'app/question_title01.py' (category 'app')."
    );
    Ok(())
}
