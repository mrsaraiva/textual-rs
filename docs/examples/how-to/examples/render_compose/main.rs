use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'render_compose' from Python source 'how-to/render_compose.py' (category 'how-to')."
    );
    Ok(())
}
