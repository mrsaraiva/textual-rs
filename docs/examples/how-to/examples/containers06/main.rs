use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'containers06' from Python source 'how-to/containers06.py' (category 'how-to')."
    );
    Ok(())
}
