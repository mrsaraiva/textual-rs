use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'containers09' from Python source 'how-to/containers09.py' (category 'how-to')."
    );
    Ok(())
}
