use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'containers08' from Python source 'how-to/containers08.py' (category 'how-to')."
    );
    Ok(())
}
