use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'containers05' from Python source 'how-to/containers05.py' (category 'how-to')."
    );
    Ok(())
}
