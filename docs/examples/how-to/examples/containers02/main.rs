use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'containers02' from Python source 'how-to/containers02.py' (category 'how-to')."
    );
    Ok(())
}
