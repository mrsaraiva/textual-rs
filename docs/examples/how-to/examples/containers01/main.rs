use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'containers01' from Python source 'how-to/containers01.py' (category 'how-to')."
    );
    Ok(())
}
