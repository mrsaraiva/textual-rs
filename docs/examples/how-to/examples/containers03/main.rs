use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'containers03' from Python source 'how-to/containers03.py' (category 'how-to')."
    );
    Ok(())
}
