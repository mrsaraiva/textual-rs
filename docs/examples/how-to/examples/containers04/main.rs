use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'containers04' from Python source 'how-to/containers04.py' (category 'how-to')."
    );
    Ok(())
}
