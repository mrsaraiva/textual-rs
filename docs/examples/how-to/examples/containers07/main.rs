use textual::prelude::*;

fn main() -> Result<()> {
    eprintln!(
        "TODO: Port docs example 'containers07' from Python source 'how-to/containers07.py' (category 'how-to')."
    );
    Ok(())
}
