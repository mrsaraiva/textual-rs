// Temporary compatibility shim while callers migrate to `crate::widgets::delegate`.
pub(crate) use crate::widgets::delegate::{
    delegate_renderable, delegate_widget_method, delegate_widget_to,
};
